- Configurare:
    OK pgpool.conf
    OK pgpool_node_id
    OK pool_hba.conf
    OK pool_passwd

OK Creare la homedir /var/lib/pgsql per utente postgres.postgres

OK Creare ssh_keys per utente postgres
OK Distribuire chiavi SSH pubbliche e private dell'utente postgres su tutti i nodi
OK Distribuire chiavi SSH pubbliche e private dell'utente postgres su tutti i nodi Postgres per utente postgres
OK Aggiungere la chiave in known_hosts per l'utente postgres su tutti i nodi

OK Aggiungere in postgres gli utenti
    OK streaming replication check
    OK health check
    OK recovery

OK Installare su proxy* il pacchetto postgresql

OK Creare su postgres* in Postgres un utente pgpool per il controllo del cluster (superuser)

OK Configurare sudoers per poter eseguire script con sudo

OK Fix ansible managed in pool_passwd

OK Copiare file eseguibili in /usr/local/bin

OK Mettere il comando /usr/local/bin/postgres-users_update.sh in cron

OK Disabilitare firewalld

Configurare pg_hba.conf sui backend PostgreSQL

File pgpass sui proxy per utente postgres per utente pgpool

Eseguire postgres_users_sync.sh alla fine dell'installazione

OK Sudoers per poter riavviare Pgpool

CREARE RUOLO:
- Configurare Logrotate

CREARE RUOLO:
OK Installare rsyslog
OK Creare directory di log /var/log/pgpool

CREARE RUOLO:
OK MSMTP
